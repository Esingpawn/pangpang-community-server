const toast=document.querySelector('.toast');document.querySelectorAll('.blueprint-card').forEach(card=>card.addEventListener('click',async e=>{if(!e.target.closest('.copy-blueprint'))return;const code=card.dataset.code;try{await navigator.clipboard.writeText(code)}catch{}toast.textContent=`分享码 ${code} 已复制`;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}));
